// functions are fun

function doMath (a, b) {
    console.log(`the result is ${a - b}`)
    console.log(`the result is ${a + b}`)
    console.log(`the result is ${a / b}`)
    console.log(`the result is ${a * b}`)
}

doMath(10, 4)
